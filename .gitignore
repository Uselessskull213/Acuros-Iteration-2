.DS_Store
**/.DS_Store
.vscode/
node_modules/
.env.local
.env.*.local

# Next.js / Vercel build artifacts
.next/
out/
next-env.d.ts
dist/
.astro/
.vercel/

# Secrets — never commit a bare .env (only *.local was ignored before)
.env

# Stray local backups / scratch files (never commit)
*.bak
*.bak*
test.txt
/hero-bg.jpg
